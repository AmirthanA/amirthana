#include<stdio.h>
main()
{
int power,m,n;
printf("\n enter the number for m:");
scanf("%d",&m);
printf("\n enter the number for n:");
scanf("%d",&n);
power=pow(m,n);
{
printf("\n the power is ,%d", power);
}
}
