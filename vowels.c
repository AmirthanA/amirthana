#include<stdio.h>
main()
{
char ch;
printf("\n enter the char:");
scanf("%c",&ch);
if(ch=='a'||ch=='A'||ch=='e'||ch=='E'||ch=='i'||ch=='I'||ch=='o'||ch=='O'||ch=='u'||ch=='U')
 {
 printf("\n the given  character is vowels:");
 }
 else
 {
  printf("\n the given character is  not a vowels:");
 }
 }
